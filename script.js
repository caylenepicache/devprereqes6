window.onload = function (){
    const pi = 3.142;

    function calcArea (r) {
        const pi = 10;
        console.log("the area is: " + pi * r * r);

    }

    console.log(pi);
    calcArea(5);
}